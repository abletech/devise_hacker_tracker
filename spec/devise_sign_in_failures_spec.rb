require 'spec_helper'

describe DeviseSignInFailures do
  it 'has a version number' do
    expect(DeviseSignInFailures::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
