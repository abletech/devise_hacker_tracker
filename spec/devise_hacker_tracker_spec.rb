require 'spec_helper'

describe DeviseHackerTracker do
  it 'has a version number' do
    expect(DeviseHackerTracker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
